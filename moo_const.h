/* $Id$
 * Copyright goes here
 * Compile time constants for openmoo
 */

/* 
 * These constants should all be present in the save file (And may even affect save format; I see some sensible ways I could/would do this); some can be ignored (the first thing I am putting in here is NAMLEN_MAX, which shouldn't affect anything, if necessary the load function should truncate)...
 */

#define NAMELEN_MAX 21
